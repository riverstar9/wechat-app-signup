#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_codepage@1.14.0@codepage/bin/codepage.njs" "$@"
  ret=$?
else 
  node  "$basedir/../../../_codepage@1.14.0@codepage/bin/codepage.njs" "$@"
  ret=$?
fi
exit $ret
