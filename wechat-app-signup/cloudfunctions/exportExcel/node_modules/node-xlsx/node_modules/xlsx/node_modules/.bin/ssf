#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_ssf@0.10.3@ssf/bin/ssf.njs" "$@"
  ret=$?
else 
  node  "$basedir/../../../_ssf@0.10.3@ssf/bin/ssf.njs" "$@"
  ret=$?
fi
exit $ret
